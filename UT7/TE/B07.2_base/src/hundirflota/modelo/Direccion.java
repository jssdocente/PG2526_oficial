package hundirflota.modelo;

/**
 * Representa las 4 direcciones posibles para colocar un barco o moverse.
 */
public enum Direccion {
    NORTE, SUR, ESTE, OESTE;

    // TODO (Opcional): Podrías añadir métodos para obtener el delta (dx, dy) de cada dirección.
    // Ejemplo: NORTE -> (-1, 0)
}
