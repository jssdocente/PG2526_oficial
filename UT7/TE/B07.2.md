# ⚓ B06.2 Hundir la Flota: Edición Orientada a Objetos

En la unidad anterior, programaste el juego "Hundir la Flota" utilizando matrices (`int[][]`) y un montón de métodos estáticos. ¡Funcionaba! Pero seamos sinceros: pasar `x` e `y` por todos lados y recordar que `6` significa "tocado" es agotador y propenso a errores.

En este reto, vamos a **rediseñar** el juego completo aplicando POO. Vamos a modelar los conceptos del juego (Barcos, Tableros, Jugadores) como **Objetos** y utilizaremos tanto **Herencia** como **Composición**.

> 📂 **Recursos**: Se proporciona la carpeta `B06.2_base` con la estructura de clases sugerida (`Direccion`, `EstadoCasilla`, `Jugador`, etc.) listas para ser implementadas.

---

## 🏗️ El Nuevo Diseño

Olvídate de `int[][] tablero` "pelado". Piensa en las entidades que forman parte del juego.

### 📍 1. La Jerarquía de Coordenadas
El átomo del juego. Vamos a hacerlo robusto usando **Herencia**.

- **Clase Abstracta `Componente`**:
    - Representa una parte de la coordenada (fila o columna).
    - Atributo: `int valor` (el índice numérico real).
    - Método: `getValue()` que devuelve ese valor.

- **Clase `Columna` extends `Componente`**:
    - Constructor: `public Columna(String letra)` -> Convierte "A" en 0, "B" en 1, etc.
    - `toString()`: Devuelve la letra ("A").

- **Clase `Fila` extends `Componente`**:
    - Constructor: `public Fila(int numero)`.
    - `toString()`: Devuelve el número.

- **Clase `Coordenada`**:
    - Atributos: `Fila fila`, `Columna columna`.
    - **Constructor**: `public Coordenada(String coordenada)` -> Parsea un string como "A5" o "C10".
        - Debe separar la letra de la columna y el número de la fila.
        - Crear los objetos `Fila` y `Columna` correspondientes.
        - **IMPORTANTE**: Internamente usamos índices 0-9 para el array, pero mostrarmos A-J y 1-10.
    - `toString()`: Devuelve "A-5".
    - `equals(Object o)`: Vital para comparar.

### 🚢 2. La Jerarquía de Barcos (Herencia... y Composición!)
Un barco ya no es un simple conjunto de números. Ahora cada tipo de navío tiene personalidad propia y, gracias a la composición, **sistemas defensivos intercambiables**.

#### 2.1 El Componente `Blindaje` (Composición)
Para no complicar la herencia con tipos como "AcorazadoReforzado" o "BuqueEvasivo", usaremos **Composición**. Todo barco *TIENE UN* blindaje.

- **Interfaz `Blindaje`**:
    - Método: `int recibirImpacto(int danioEntrante)`. Devuelve el daño que *realmente* sufre el barco tras intentar mitigar el golpe.

- **Implementaciones**:
    - `BlindajeSimple`: No hace nada. Devuelve el daño tal cual.
    - `BlindajeReforzado`: Absorbe el primer impacto (devuelve 0) y luego se rompe (pasa a comportarse como uno simple).
    - `BlindajeEvasivo`: Tiene un 20% de probabilidad de esquivar totalmente el daño (devuelve 0).

#### 2.2 clase Abstracta `Barco`
    - **Atributos**:
        - `nombre` (String).
        - `tamaño` (int).
        - `vidas` (int).
        - `cargasHabilidad` (int): Número de veces que puede usar su habilidad especial.
        - `blindaje` (Blindaje): **Nuevo atributo** para la defensa.
    - **Constructor**: Recibe nombre, tamaño, blindaje, etc.
    - **Métodos Concretos**:
        - `recibirImpacto()`: **Modificado**. No resta vida directamente. Delega en `blindaje.recibirImpacto(1)` y solo resta vida si el resultado es > 0.
        - `estaHundido()`: Devuelve `true` si las vidas llegan a 0.
    - **Método Abstracto**:
        - `public abstract void activarHabilidadEspecial(Tablero tableroObjetivo, Coordenada objetivo);`

#### Tipos de Barcos (Subclases)
Debes implementar las siguientes clases concretas. Cada una sobrescribe `activarHabilidadEspecial`:

| Clase | Tamaño | Cargas | Habilidad Especial | Efecto |
| :--- | :---: | :---: | :--- | :--- |
| **`Portaaviones`** | 5 | 1 | **Reconocimiento Aéreo** | Revela el estado (Agua/Barco) de un área de **3x3** alrededor del objetivo sin dañar. |
| **`Buque`** | 4 | 1 | **Ataque en Cruz** | Dispara a **toda la fila y toda la columna** de la coordenada indicada. |
| **`Submarino`** | 3 | 1 | **Inmersión** | Se vuelve invulnerable a los disparos rivales durante el **siguiente turno**. |
| **`Acorazado`** | 4 | 1 | **Bombardeo en Zona** | Impacta en la coordenada objetivo y en las 4 casillas adyacentes (+). |
| **`Destructor`** | 2 | 2 | **Disparo Doble** | Permite disparar de nuevo en el mismo turno. |

---

### 🗺️ 3. La Clase `Tablero` e Interfaces

Es el responsable de organizar el juego.

#### Interfaz `Atacable`
Define el contrato para recibir disparos:

```java
public interface Atacable {
    ResultadoDisparo recibirDisparo(Coordenada c);
}
```

#### Clase `Tablero` implements `Atacable`
- **Estructura Interna**:
    - Una matriz de **Casillas**: `private Casilla[][] casillas = new Casilla[10][10];`
    - **Clase `Casilla`** (Clase interna o externa):
        - `EstadoCasilla estado`: Enum (`AGUA`, `AGUA_DISPARADA`, `BARCO`, `TOCADO`).
        - `Barco contenido`: Referencia al objeto Barco (null si no hay).
- **Responsabilidades**:
    - `colocarBarco(Barco b, Coordenada inicio, Direccion d)`: Valida límites y superposiciones.
    - `recibirDisparo(Coordenada c)`: Gestiona el impacto y actualiza el estado de la casilla y el barco.
    - `mostrar(boolean oculto)`: Imprime el tablero. Si `oculto` es true, no muestra los barcos intactos (vista del rival).

---

### 👤 4. La Clase `Jugador`
Aquí gestionamos la inteligencia y el flujo.

- **Clase `Jugador`** (Puede ser abstracta):
    - **Atributos**:
        - `miFlota`: Su propio `Tablero`.
        - `radar`: Referencia al `Tablero` del enemigo (para atacar) o una matriz propia para anotar resultados.
        - `List<Barco> barcos`: Lista polimórfica con todos sus barcos para gestionar las cargas de habilidades.
    - **Método Abstracto**: `Coordenada elegirDisparo()`.

- **`JugadorHumano`**:
    - Pide coordenandas por teclado.
    - Puede elegir: "Disparar normal" o "Usar Habilidad de [Barco]".

- **`JugadorMaquina`**:
    - Dispara aleatoriamente (evitando repetidos).
    - (Opcional) Usa habilidades si tiene cargas disponibles de forma inteligente.

---

## 🤝 Trabajo en Parejas (Recomendado)

Como este reto en parejas, os sugerimos dividir el trabajo en dos roles independientes para poder programar en paralelo sin pisaros el código. Usad el fichero `TestManual.java` para probar vuestra parte.

### 👩‍💻 Rol A: Ingeniero de Estructuras (El Escenario)
Tu responsabilidad es crear el "mundo" donde ocurre la batalla y asegurar que las coordenadas son correctas.
-   **Ficheros a tu cargo**: `Componente.java`, `Fila.java`, `Columna.java`, `Coordenada.java`, `Tablero.java`, `Casilla.java`.
-   **Tu Meta**: Lograr que `testCoordenadas()` funcione y que el `Tablero` se inicialice y se muestre por pantalla correctamente.

### 👨‍✈️ Rol B: Almirante de la Flota (Los Actores)
Tu responsabilidad es crear las unidades de combate y sus sistemas de defensa.
-   **Ficheros a tu cargo**: `Blindaje` (y sus 3 implementaciones), `Barco.java` y las 5 subclases de barcos (`Submarino`, `Acorazado`, etc.).
-   **Tu Meta**: Lograr que `testBlindajes()` y `testBarcos()` funcionen, verificando que los barcos absorben daño según su blindaje.

*Al final, juntad vuestro código en `Juego.java` para ver cómo los barcos del Almirante navegan en el tablero del Ingeniero.*

---

## 🎮 El Flujo del Juego (Clase Main)

El bucle principal se beneficia del polimorfismo:

```java
public void jugar() {
    // Inicialización...
    while (j1.tieneBarcos() && j2.tieneBarcos()) {
        // Turno J1
        // 1. Elegir acción (Disparo normal o Habilidad)
        // 2. Ejecutar acción sobre tablero rival
        // 3. Mostrar resultado
        // 4. Cambio turno
    }
}
```

## 📝 Tu Misión
1.  **Rescata** la lógica de Coordenadas (Fila, Columna).
2.  **Implementa** el sistema de defensa con la interfaz `Blindaje` y sus 3 estrategias.
3.  **Implementa** la Jerarquía Naval (`Barco` abstracto + 5 tipos), inyectando blindajes diferentes a cada tipo de barco (ej: Submarinos con Evasivo, Acorazados con Reforzado).
4.  **Gestiona** el Tablero con la matriz de `Casilla` y la interfaz `Atacable`.
5.  **Disfruta** viendo cómo el Polimorfismo maneja las habilidades especiales mágicamente.
