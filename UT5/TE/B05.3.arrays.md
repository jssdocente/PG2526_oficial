# UT05. Bloque 3. 🧠 Retos avanzados con Arrays

| Resultados Aprendizaje | CE |
| ----------------------------------- | ----------- |
| RA6             | CE3a, CE3c, CE3d |

---

### Ejercicio 1: Matriz de Sumas Parciales 🧾

Escribe un programa que genere al azar **20 números enteros** comprendidos entre 0 y 9. Estos números se deben introducir en una matriz de **4 filas por 5 columnas**.

El reto es que la matriz final debe tener una fila y una columna más para almacenar las **sumas parciales**.

*   La última columna guardará la suma de cada fila.
*   La última fila guardará la suma de cada columna.
*   La celda de la esquina inferior derecha almacenará la **suma total** de todos los números.

### Ejercicio 2: Filtrando por el Último Dígito 🔎

Crea un programa que haga lo siguiente:

1.  Pide al usuario un tamaño para un array de números.
2.  Rellena el array con números aleatorios entre 1 y 300.
3.  Pide al usuario que introduzca un **dígito** (de 0 a 9).
4.  Busca en el array todos los números que **acaben en ese dígito** y guárdalos en un nuevo array.
5.  Finalmente, muestra el array con los números encontrados.

Por ejemplo, si el usuario elige el dígito `5`, el programa podría encontrar números como `25`, `155`, `85`, etc.

### Ejercicio 3: Inserción y Desplazamiento ➡️

Crea un array de 100 elementos, inicialmente vacío. El programa pedirá al usuario que introduzca números por teclado. La mecánica es la siguiente:

*   Cada nuevo número introducido se colocará en la **posición 0** del array.
*   Esto obliga a que todos los elementos que ya estaban en el array se **desplacen una posición a la derecha**.

Cuando el usuario introduzca el número `0`, el programa finalizará y mostrará el contenido útil del array (todos los números que se han introducido).

### Ejercicio 4: Simulación Iterativa 🔄

Crea un array de **150 números aleatorios** comprendidos entre 5 y 10.
El programa pedirá por teclado un **número de iteraciones**.

En cada iteración, debes recorrer el array y actualizar el valor de cada celda según la siguiente regla: el nuevo valor de una celda será la suma del valor de la celda **anterior** más el valor de la celda **siguiente**.

**¡Cuidado!** La primera y la última celda no se pueden actualizar porque no tienen dos "vecinos". Además, para que los cálculos sean correctos, deberás usar los valores originales del array en cada iteración, no los que ya has modificado.