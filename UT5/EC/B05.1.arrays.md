# UT05. Bloque 1. Ejercicios: Trabajo con Arrays

## Nivel 1: Primeros Pasos con Vectores 👣

### Ejercicio 1 🔢

Realiza un programa que defina un vector llamado `vector_numeros` de 10 enteros. A continuación, inicialízalo con valores aleatorios (del 1 al 10) y, posteriormente, muestra en pantalla cada elemento del vector junto con su **cuadrado** y su **cubo**.

### Ejercicio 2 🔄

Crea un vector de 5 elementos de cadenas de caracteres. Inicializa el vector con datos leídos por teclado ⌨️. Copia los elementos del vector en otro vector pero en **orden inverso**, y muéstralo por la pantalla.

### Ejercicio 3 🎓

Se quiere realizar un programa que lea por teclado las 5 notas obtenidas por un alumno (comprendidas entre 0 y 10). A continuación, debe mostrar:

*   Todas las notas.
*   La nota media 📊.
*   La nota más alta 📈.
*   La nota más baja 📉.

### Ejercicio 4 🛑

Programa que declare un vector de diez elementos enteros y pida números para rellenarlo hasta que se llene el vector o se introduzca un **número negativo**. Al terminar, se debe imprimir el vector (mostrando solo los elementos que se han introducido).

### Ejercicio 5 ➕

Programa que declare tres vectores: `vector1`, `vector2` y `vector3` de cinco enteros cada uno. Pide valores para `vector1` y `vector2` por teclado y calcula `vector3` como la suma de los dos anteriores (`vector3 = vector1 + vector2`).

---

## Ampliación: Trabajando con Arrays 🛠️

### Ejercicio 1 🧑‍🎓

Queremos guardar los nombres y las edades de los alumnos de un curso. Realiza un programa que introduzca el nombre y la edad de cada alumno. 
El proceso de lectura de datos terminará cuando se introduzca como nombre un asterisco (`*`). Al finalizar se mostrará:

*   Todos los alumnos que son **mayores de edad**.
*   El/los alumno/s de **mayor edad** 🎂.

### Ejercicio 2 🌡️

Queremos guardar la temperatura mínima y máxima de 5 días. Realiza un programa que ofrezca la siguiente información:

*   La temperatura media de cada día ⚖️.
*   Los días con la temperatura más baja ❄️.
*   Se lee una temperatura por teclado y se muestran los días cuya temperatura máxima coincide con ella. Si no existe ningún día, se muestra un mensaje informativo.

### Ejercicio 3 🧮

Diseña el algoritmo para un programa que:

1.  Cree una tabla bidimensional (matriz) de 5x5 llamada `matriz`.
2.  Cargue la tabla con valores numéricos enteros.
3.  **Sume todos los elementos de cada fila y todos los elementos de cada columna**, visualizando los resultados en pantalla.

### Ejercicio 4 📐

Diseña el algoritmo para un programa que:

1.  Cree una tabla bidimensional de 5x5 llamada `diagonal`.
2.  Cargue la tabla de forma que los componentes de la **diagonal principal** tomen el valor `1` y el resto el valor `0`.
3.  Muestre el contenido de la tabla en pantalla.

### Ejercicio 5 🖼️

Diseña el algoritmo para un programa que:

1.  Cree una tabla bidimensional de 5x15 llamada `marco`.
2.  Cargue la tabla con ceros y unos para que se dibuje un **marco**. El valor `1` debe ocupar las posiciones más externas, y el resto de los elementos (`0`) deben quedar en el interior.
3.  Visualice el contenido de la matriz en pantalla, que debería tener este aspecto:
   
    ```
    111111111111111
    100000000000001
    100000000000001
    100000000000001
    111111111111111
    ```