# ⚔️ EC06.1 Introducción a la POO

¡Bienvenido a la Programación Orientada a Objetos (POO)! 
Olvídate de pensar en pasos secuenciales ("haz esto, luego esto"). En POO, pensamos en **cosas** (objetos) que tienen características y acciones.

Imagina que eres un dios creador de videojuegos. 🌍
- Tienes el "código genético" de un héroe (la **Clase**).
- Con ese código, puedes crear cientos de héroes puntuales (los **Objetos**).

> 🍪 **La Metáfora de la Galleta**:
> - La **Clase** es el **molde**. Define la forma, pero no se come. Solo hay uno.
> - El **Objeto** es la **galleta** real. Sale del molde. Puedes crear infinitas y cada una puede estar un poco más tostada o tener más pepitas de chocolate, pero todas son galletas.

---

## 🛡️ Ejercicio 1: El Molde (Definiendo la Clase)
*Creando los planos de nuestro Héroe.*

Vamos a crear nuestra primera clase en Java. Queremos representar a un héroe de rol.
Todos los héroes tendrán:

- Un nombre.
- Un nivel (empieza en 1).
- Puntos de Salud (vida).
- Un rol (ej: "Mago", "Guerrero").

### 📝 Tu tarea:

1. Crea un nuevo archivo llamado `Heroe.java`.
2. Define la clase `Heroe`.
3. Añade los **atributos** (variables) necesarios. Piensa qué tipo de dato necesita cada uno (`String`, `int`...).

> 💡 **Consejo**: Por ahora, usa `int` para nivel y salud. No te compliques con la privacidad (`private`/`public`) todavía, vamos a dejarlo simple (`default` o `public`) para empezar, aunque en el futuro aprenderemos a protegerlos.

---

## 🔮 Ejercicio 2: ¡Invocación! (Creando Objetos)
*Dando vida al molde.*

De nada sirve tener el plano del héroe si no creamos ninguno. Vamos a crear a nuestros protagonistas.

### 📝 Tu tarea:
1. Crea una clase `Main` con un método `main`.
2. Dentro del `main`, instancia (crea) dos objetos de la clase `Heroe`.
   - Ejemplo: `Heroe h1 = new Heroe();`
3. Asigna valores a sus atributos manualmente unz vez creados.
   - Al primero llámalo "Gandalf", rol "Mago", salud 100.
   - Al segundo "Aragorn", rol "Guerrero", salud 150.
4. Muestra por consola el nombre y la salud de ambos.

### 🧩 Reto Extra (Constructores):
Asignar los datos línea a línea es aburrido (`h1.nombre = ...; h1.salud = ...`).
¿Y si pudiéramos darles los datos al nacer?
- Crea un **Constructor** en la clase `Heroe` que reciba el nombre y el rol.
- Haz que el `nivel` empiece siempre en 1 y la `salud` en 100 automáticamente dentro del constructor.
- Ahora, en el `main`, ¡crea los héroes en una sola línea!

---

## ⚡ Ejercicio 3: ¡Acción! (Métodos)
*Los objetos no son solo datos, ¡hacen cosas!*

Nuestros héroes son muy estáticos. Vamos a darles habilidades. En POO, las funciones que viven dentro de una clase se llaman **métodos**.

### 📝 Tu tarea:
Vuelve a la clase `Heroe` y añade los siguientes métodos:

1. `presentarse()`: No recibe nada, no devuelve nada (`void`). Simplemente imprime: 
   *"Hola, soy [nombre] y soy un nivel [nivel] [rol]"*.
2. `subirNivel()`: Cada vez que se llama:
   - Aumenta el nivel en 1.
   - Aumenta la salud en 10 puntos.
   - Imprime: *"[nombre] ha subido al nivel [nuevoNivel]!"*

En el `Main`, prueba a llamar a estos métodos con tus héroes creados. ¡Sube de nivel a uno de ellos varias veces!

---

## 🧪 Ejercicio 4: Interacción (Objetos vs Objetos)
*Cuando los mundos colisionan.*

La magia de la POO es cuando los objetos interactúan entre sí.

### 📝 Tu tarea:
1. Añade un método `beberPocion()` a `Heroe`. Suma 20 de salud, pero ojo, ¡no dejes que pase de 100 si ese es el máximo! (Usa un `if`).
2. **El gran reto**: Añade un método `atacar(Heroe objetivo)`.
   - Este método recibe **otro objeto** `Heroe` como parámetro.
   - Resta 10 puntos de salud al `objetivo`.
   - Imprime: *"[nombre] ataca a [nombre del objetivo] y le hace 10 de daño!"*.

En tu `Main`:
- Haz que Gandalf ataque a Aragorn.
- Muestra la salud de Aragorn para ver si ha bajado.
- Haz que Aragorn beba una poción para recuperarse.

---

> 🎓 **Resumen de lo aprendido**:
> 1. **Clase**: El plano (`Heroe.java`).
> 2. **Objeto**: La instancia viva (`new Heroe()`).
> 3. **Atributo**: Datos del objeto (`nombre`, `salud`).
> 4. **Método**: Comportamiento del objeto (`atacar()`).
> 5. **Constructor**: El código que prepara al objeto al nacer.
