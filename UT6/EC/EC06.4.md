# 🛒 EC06.4 Supermercado: Creación de Interfaces

En este ejercicio vamos a modelar los productos de un supermercado utilizando **Interfaces** en Java. El objetivo es definir contratos de comportamiento (`EsLiquido`, `EsAlimento`) que distintas clases (`Detergente`, `Vino`, `Cereales`) deberán cumplir.

---

## 📋 1. Definición de Interfaces

Para unificar el código, debes crear las siguientes interfaces que describen características específicas de los productos.

### 💧 Interfaz `EsLiquido`
Indica que un objeto es líquido y tiene volumen y tipo de envase.

```java
public interface EsLiquido {
    public void setVolumen(double v);
    public double getVolumen();
    public void setTipoEnvase(String env);
    public String getTipoEnvase();
}
```

### 🍎 Interfaz `EsAlimento`
Indica que un objeto es un alimento comestible.

```java
import java.time.LocalDate;

public interface EsAlimento {
    public void setCaducidad(LocalDate fc);
    public LocalDate getCaducidad();
    public int getCalorias();
}
```

### 🏷️ Interfaz `ConDescuento`
Indica que el producto es susceptible de tener ofertas.

```java
public interface ConDescuento {
    public void setDescuento(double des);
    public double getDescuento();
    public double getPrecioDescuento();
}
```

---

## 📦 2. Clases de Productos

Debes programar las siguientes clases implementando las interfaces necesarias.

### 🧴 Clase `Detergente`

Representa una botella de detergente.

- **Interfaces**: `EsLiquido`, `ConDescuento`.
- **Propiedades**: `marca` (String), `precio` (double).
    - *Añade las propiedades extra necesarias para implementar los métodos de las interfaces (ej: `volumen`, `envase`, `descuento`).*
- **Constructor**: Recibe `marca` y `precio`.
- **Métodos**:
    - Getters y Setters de las propiedades propias.
    - Implementación de todos los métodos de las interfaces.
    - `toString()`: Devuelve una cadena con toda la información.

### 🥣 Clase `Cereales`

Representa una caja de cereales.

- **Interfaces**: `EsAlimento`. *(NO tiene descuento)*.
- **Enum `TipoCereal`**: Puede ser `ESPELTA`, `MAIZ`, `TRIGO`, `OTRO`.
- **Propiedades**: `marca` (String), `precio` (double), `tipo` (TipoCereal).
- **Constructor**: Recibe marca, precio y tipo.
- **Lógica de Calorías**:
    - Espelta: 5 cal.
    - Maíz: 8 cal.
    - Trigo: 12 cal.
    - Otro: 15 cal.
- **Métodos**:
    - Implementación de `EsAlimento`.
    - `toString()` completo.

### 🍷 Clase `Vino`

Representa una botella de vino.

- **Interfaces**: `EsLiquido`, `EsAlimento`, `ConDescuento`. *(¡Implementa las tres!)*.
- **Propiedades**: `marca`, `tipoVino`, `gradosAlcohol`, `precio`.
- **Constructor**: Recibe las 4 propiedades principales.
- **Lógica de Calorías**:
    - Las calorías se calculan como `gradosAlcohol * 10`.
- **Métodos**:
    - Implementación de todos los métodos de las interfaces.
    - `toString()` completo.

---

## 🏪 3. Clase `Supermercado` (Principal)

Esta clase gestionará el inventario.

- **Inventario**: Debe tener un `ArrayList` (o array) con todos los productos.
- **Métodos**:
    - `añadirProducto(Object p)`: Añade un producto a la lista.
    - `imprimirInventario()`: Recorre la lista y muestra cada producto.
        - Además, debe detectar de qué tipo es cada producto (usando `instanceof`) e imprimir sus características especiales:
            - Si `EsLiquido`: Imprimir "Envase: ..."
            - Si `EsAlimento`: Imprimir "Calorías: ..."
            - Si `ConDescuento`: Imprimir "Precio final: ..."



